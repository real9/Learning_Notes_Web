import Vue from 'vue'
import Vuex from 'vuex'
import evaluationResults from "@/store/modules/evaluationResults";

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    evaluationResults,
  }
})
